services:
  action:
    image: public.ecr.aws/infra-blocks/docker-rust-action-template:latest
    build:
      context: ../
      dockerfile: docker/action/Dockerfile
    container_name: docker-rust-action-template
